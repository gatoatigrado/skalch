#!/bin/bash
# environment variables (source it from your .bashrc?)

export skalchdir="$(dirname "$(readlink -f "$0")")"
export grgenfiles="$skalchdir"/plugin/src/main/grgen

function g() { grep -E "$@" **/*(grg|grs) | sed '/\/gen\//d'; }
